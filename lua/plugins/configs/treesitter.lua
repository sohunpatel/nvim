local options = {
  ensure_installed = { "lua", "c", "cpp", "python", "robot", "rust", "verilog", "vim" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
